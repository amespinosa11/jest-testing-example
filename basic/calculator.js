const add = (a, b) => a + b;
const subtract = (a, b) => a - b;
const multiply = (a, b) => a * b;
const divide = (a, b) => a / b;

const Calculator = {
  add,
  subtract,
  multiply,
  divide,
};

module.exports = Calculator;
